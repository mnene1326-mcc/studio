rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Open rules for development: allow any authenticated user to read and write
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}