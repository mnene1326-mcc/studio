rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Development rules: Allow all authenticated users to read and write to any document.
    // This comment triggers a redeployment to ensure rules are active.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}