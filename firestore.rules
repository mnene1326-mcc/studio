rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // FULLY OPEN RULES - ALLOW ALL READS AND WRITES
    // This resolves all 'missing or insufficient permissions' errors during development.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}