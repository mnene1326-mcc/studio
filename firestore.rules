rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Development rules: Allow all authenticated users to read and write to any document.
    // Triggering redeployment to resolve persistent permission errors.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}